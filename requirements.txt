Flask>=2.2
flask-cors>=4.0
python-dotenv>=1.0.0

requests>=2.0.0
beautifulsoup4>=4.10
lxml>=4.9
APScheduler>=3.10

# MongoDB client (optional)
pymongo>=4.0
dnspython>=2.0

# Production WSGI server (recommended for deployment)
gunicorn>=20.1

# ML deps (needed for local /predict image preprocessing)
numpy
Pillow

# TensorFlow is only needed if you want to run the bundled model locally.
# Render currently defaults to Python 3.13, and tensorflow==2.16.1 has no wheels for 3.13.
# On Python 3.13+ the app will run without local ML and will use Gemini for /predict.
tensorflow==2.16.1; python_version < "3.13"
