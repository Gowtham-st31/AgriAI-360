Flask>=2.2
flask-cors>=4.0
python-dotenv>=1.0.0

requests>=2.0.0
beautifulsoup4>=4.10
lxml>=4.9
APScheduler>=3.10

# MongoDB client (optional)
pymongo>=4.0
dnspython>=2.0

# Production WSGI server (recommended for deployment)
gunicorn>=20.1

# Optional ML deps (only needed for /predict)
# numpy
# Pillow
# tensorflow
